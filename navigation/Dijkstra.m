classdef Dijkstra < handle
    properties (SetAccess=public, GetAccess=public)
        map
        
        dist_mat % Distance matrix. dist_mat(i,j) = distance between edges i and j
        
        
    end
end
